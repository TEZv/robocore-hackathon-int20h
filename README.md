# 🤖 SKELAR AI — Support Chat Analyzer

> Система автоматичної генерації та інтелектуального аналізу діалогів служби підтримки на базі Gemini AI

---

## 📌 Про проєкт

Цей проєкт вирішує реальну бізнес-задачу: **автоматизація аналізу якості роботи служби підтримки**.

Система складається з двох скриптів:

| Скрипт | Що робить |
|--------|-----------|
| `generate.py` | Генерує реалістичний датасет чатів між клієнтом і агентом підтримки |
| `analyze.py` | Аналізує кожен діалог і повертає структурований JSON з оцінками |

### 🎯 Що аналізується:
- **Intent** — тема звернення (`payment_issue`, `tech_error`, `account_access`, `tariff_question`, `refund`, `other`)
- **Satisfaction** — рівень задоволеності клієнта (`satisfied` / `neutral` / `unsatisfied`)
- **Quality Score** — оцінка роботи агента від 1 до 5
- **Agent Mistakes** — список помилок агента (`ignored_question`, `incorrect_info`, `rude_tone`, `no_resolution`, `unnecessary_escalation`)

### 🥚 Пасхалки в датасеті:
Система вміє виявляти **приховану незадоволеність** — коли клієнт формально дякує, але проблема насправді не вирішена.

---

## 💡 Цінність рішення та подальша реалізація

### Навіщо це потрібно бізнесу?

Служби підтримки обробляють сотні та тисячі чатів щодня. Читати їх вручну — неможливо. Ця система вирішує три конкретні проблеми:

| Проблема | Рішення |
|----------|---------|
| Менеджер не знає, які агенти працюють погано | `quality_score` + `agent_mistakes` по кожному чату |
| Клієнт формально закрив тікет, але незадоволений | Виявлення прихованої незадоволеності (`easter_egg`) |
| Незрозуміло, які теми звернень найчастіші | Автоматична класифікація `intent` |

### 📊 Що робити з results.json далі?

`results.json` — це структуровані дані, готові до подальшого використання:

**1. Дашборд для менеджера підтримки**
Завантажити results.json у BI-інструмент (Power BI, Tableau, Metabase, навіть Google Sheets)
і отримати живий дашборд: середній quality_score по агентам, топ помилок, динаміка задоволеності.

**2. Автоматичні алерти**
Тригерити сповіщення коли quality_score < 3 або виявлено hidden dissatisfaction —
менеджер одразу бачить проблемний кейс і може втрутитись.

**3. Навчання агентів**
Автоматично формувати звіт: "За тиждень агент Іван допустив 12 помилок типу ignored_question" —
персоналізований фідбек без ручного review.

**4. Fine-tuning власної моделі**
Згенерований датасет (dataset.json) можна використати як тренувальні дані
для навчання спеціалізованої моделі класифікації підтримки — дешевшої та швидшої за GPT.

**5. Інтеграція в реальний pipeline**
```
Реальні чати (CRM/Zendesk/Intercom)
        ↓
    analyze.py (або API-обгортка навколо нього)
        ↓
    results.json / БД
        ↓
Дашборд + Алерти + Звіти для HR
```

### 🚀 Roadmap подальшого розвитку

```
v1.0 (зараз)       Генерація датасету + базовий аналіз через Gemini
      ↓
v1.1               REST API обгортка навколо analyze.py (FastAPI)
                   — щоб приймати реальні чати з CRM в реальному часі
      ↓
v1.2               Дашборд (Streamlit або Grafana)
                   — візуалізація результатів без BI-інструментів
      ↓
v2.0               Інтеграція з Zendesk / Intercom / власною CRM
                   — повністю автоматичний pipeline без ручного запуску
      ↓
v2.1               Fine-tuned класифікатор на базі згенерованого датасету
                   — менша латентність, менші витрати на API
```

---

## 🗂️ Структура проєкту

```
robocore-hackathon-int20h/
├── generate.py       # Генерація діалогів через Gemini API
├── analyze.py        # Аналіз діалогів, повертає JSON
├── dataset.json      # Згенеровані діалоги (створюється автоматично)
├── results.json      # Результати аналізу (створюється автоматично)
├── Dockerfile        # Docker-конфігурація
├── requirements.txt  # Залежності
├── .env.example      # Приклад конфігурації (без секретів)
└── README.md
```

---

## ⚙️ Швидкий старт

### Крок 1 — Клонуй репозиторій

```bash
git clone https://github.com/TEZv/robocore-hackathon-int20h
cd robocore-hackathon-int20h
```

### Крок 2 — Налаштуй API ключ

Створи файл `.env` у корені проєкту:

```
GEMINI_API_KEY=your_gemini_api_key_here
```

> 🔑 Отримати безкоштовний ключ: https://aistudio.google.com/app/apikey

---

## 🐍 Запуск через Python

```bash
# Встанови залежності
pip install -r requirements.txt

# 1. Згенеруй датасет чатів
python generate.py

# 2. Проаналізуй згенеровані чати
python analyze.py
```

---

## 🐳 Запуск через Docker

```bash
# Збери образ
docker build -t robocore-hackathon-int20h .

# 1. Згенеруй датасет (збереже dataset.json у поточну папку)
docker run --env-file .env -v ${PWD}:/app robocore-hackathon-int20h python generate.py

# 2. Проаналізуй датасет (збереже results.json у поточну папку)
docker run --env-file .env -v ${PWD}:/app robocore-hackathon-int20h python analyze.py
```

> **Windows CMD:** замість `${PWD}` використовуй `%cd%`

---

## 📄 Формат даних

### dataset.json (вихід generate.py)

```json
[
  {
    "id": "payment_issue",
    "type": "normal",
    "messages": [
      {"role": "client", "text": "Добрий день, моя картка не проходить оплату"},
      {"role": "agent",  "text": "Вітаю! Зараз розберемось..."}
    ]
  }
]
```

### results.json (вихід analyze.py)

```json
[
  {
    "chat_id": "payment_issue",
    "chat_type": "normal",
    "analysis": {
      "intent": "payment_issue",
      "satisfaction": "satisfied",
      "quality_score": 5,
      "agent_mistakes": [],
      "reasoning": "Агент швидко і ввічливо вирішив проблему з оплатою"
    }
  }
]
```

---

## 🧪 Типи сценаріїв у датасеті

| Тип | Опис | Приклади |
|-----|------|---------|
| `normal` | Успішні кейси | Агент вирішує проблему, клієнт задоволений |
| `conflict` | Конфліктні кейси | Клієнт злий, агент допускає помилки |
| `problem` | Проблемні кейси | Груба відповідь, неправильна інформація |
| `easter_egg` | Прихована незадоволеність | Клієнт дякує, але проблема не вирішена |

---

## 📦 Залежності

```
google-generativeai
python-dotenv
```

---

## 🔐 Безпека

- Ніколи не комітити `.env` у репозиторій
- Файл `.env` додано до `.gitignore`
- Використовуй `.env.example` як шаблон для інших розробників

---

## 👥 Команда

Розроблено в рамках хакатону **int20h** — SKELAR AI Test Task

---

*© 2026 robocore-hackathon-int20h*
